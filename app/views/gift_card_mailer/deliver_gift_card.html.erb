<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>You've received a REM gift card!</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #f8f9fa;
    }
    .container {
      background: white;
      border-radius: 12px;
      padding: 40px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
    }
    .gift-icon {
      font-size: 48px;
      margin-bottom: 20px;
    }
    .amount {
      font-size: 32px;
      font-weight: bold;
      color: #10b981;
      margin: 20px 0;
    }
    .sender-info {
      background: #f3f4f6;
      padding: 20px;
      border-radius: 8px;
      margin: 20px 0;
    }
    .cta-button {
      display: inline-block;
      background: #10b981;
      color: white;
      padding: 16px 32px;
      text-decoration: none;
      border-radius: 8px;
      font-weight: bold;
      margin: 20px 0;
      text-align: center;
    }
    .otp-code {
      background: #1f2937;
      color: white;
      padding: 20px;
      border-radius: 8px;
      text-align: center;
      font-size: 24px;
      font-weight: bold;
      letter-spacing: 4px;
      margin: 20px 0;
    }
    .qr-code {
      text-align: center;
      margin: 30px 0;
    }
    .expiry-info {
      background: #fef3c7;
      border: 1px solid #f59e0b;
      padding: 16px;
      border-radius: 8px;
      margin: 20px 0;
    }
    .footer {
      text-align: center;
      margin-top: 40px;
      padding-top: 20px;
      border-top: 1px solid #e5e7eb;
      color: #6b7280;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="gift-icon">🎁</div>
      <h1>You've received a REM gift card!</h1>
    </div>

    <div class="amount">
      <%= @gift_card.currency %> <%= @gift_card.amount / 100.0 %>
    </div>

    <div class="sender-info">
      <strong>From:</strong> <%= @sender.name %><br>
      <strong>Email:</strong> <%= @sender.email %>
    </div>

    <div class="otp-code">
      Your redemption code: <%= @tokens[:otp] %>
    </div>

    <div style="text-align: center;">
      <a href="<%= @redeem_url %>" class="cta-button">
        Redeem Your Gift Card
      </a>
    </div>

    <% if @qr_code %>
      <div class="qr-code">
        <h3>Or scan this QR code:</h3>
        <%= raw @qr_code %>
      </div>
    <% end %>

    <div class="expiry-info">
      <strong>⏰ Important:</strong><br>
      • Your redemption link expires in <strong>7 days</strong><br>
      • Your redemption code expires in <strong>1 hour</strong><br>
      • Use both the link and code to redeem your gift card
    </div>

    <div style="margin: 30px 0; padding: 20px; background: #f9fafb; border-radius: 8px;">
      <h3>How to redeem:</h3>
      <ol>
        <li>Click the "Redeem Your Gift Card" button above or scan the QR code</li>
        <li>Enter your 6-digit redemption code: <strong><%= @tokens[:otp] %></strong></li>
        <li>Choose a merchant to redeem at</li>
        <li>Enjoy your gift card!</li>
      </ol>
    </div>

    <div class="footer">
      <p>Thank you for using REM! 🚀</p>
      <p>If you have any questions, please contact our support team.</p>
    </div>
  </div>
</body>
</html>
